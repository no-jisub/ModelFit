---
const { hasAffiliateLinks = false } = Astro.props as { hasAffiliateLinks?: boolean };
const hasConfiguredAffiliateBase = Boolean(import.meta.env.PUBLIC_COUPANG_BASE_URL?.trim());
const requiredDisclosure =
  "이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.";
const affiliateActive = hasAffiliateLinks || hasConfiguredAffiliateBase;
const nonAffiliateText =
  "현재 쿠팡 링크는 일반 상품·검색 링크이며 모델핏은 수수료를 받지 않습니다. 상품명만으로 정품을 단정하지 말고 부품번호, 판매자와 제조사 표기를 확인하세요.";
const text = affiliateActive
  ? requiredDisclosure
  : import.meta.env.PUBLIC_AFFILIATE_DISCLOSURE_TEXT?.trim() || nonAffiliateText;
---

<p class="affiliate-disclosure"><span aria-hidden="true">ⓘ</span> {text}</p>
